---
import { getLangFromUrl, useTranslations, getLocalizedPath } from '../i18n/utils';

const currentYear = new Date().getFullYear();
const blogStartYear = 2024;
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<footer class="mt-16 border-t border-[var(--color-border)]">
  <div class="mx-auto max-w-4xl px-4 py-8">
    <div class="flex flex-col items-center gap-4">
      <div class="flex gap-4 text-sm">
        <a 
          href="https://github.com/vremyavnikuda" 
          target="_blank" 
          rel="noopener noreferrer"
          class="text-[var(--color-text-secondary)] hover:text-primary-400 transition-colors"
        >
          {t('footer.github')}
        </a>
        <a 
          href={getLocalizedPath('/privacy', lang)}
          class="text-[var(--color-text-secondary)] hover:text-primary-400 transition-colors"
        >
          {t('footer.privacy')}
        </a>
      </div>
      <p class="text-[var(--color-text-secondary)] text-sm">
        © {blogStartYear}-{currentYear} vremyavnikuda
      </p>
    </div>
  </div>
</footer>
