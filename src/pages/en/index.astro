---
import BaseLayout from '@layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const lang = 'en';

const allPosts = (await getCollection('blog'))
  .filter(post => !post.data.draft && post.data.lang === lang)
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 5);
---

<BaseLayout>
  <div class="max-w-4xl mx-auto px-4 py-16">
    <section class="mb-24">
      <h1 class="text-4xl md:text-5xl font-bold mb-6">
        Hi, I'm vremyavnikuda
      </h1>
      <p class="text-xl text-[var(--color-text-secondary)] mb-8 max-w-2xl">
        I'm a Systems Software Developer specializing in building components that ensure the stability and reliability of large-scale infrastructure. This is my personal space where I share knowledge, opinions, and information that I find interesting and worth exploring.
      </p>
      <div class="flex flex-col sm:flex-row gap-4">
        <a 
          href="/en/blog" 
          class="px-6 py-3 bg-primary-400 text-slate-950 font-medium rounded-lg hover:bg-primary-300 transition-colors w-full sm:w-auto text-center"
        >
          Read the blog
        </a>
        <a 
          href="/en/projects" 
          class="px-6 py-3 border border-[var(--color-border)] rounded-lg hover:bg-[var(--color-bg-secondary)] transition-colors w-full sm:w-auto text-center"
        >
          View projects
        </a>
        <a 
          href="/en/achievements" 
          class="px-6 py-3 border border-[var(--color-border)] rounded-lg hover:bg-[var(--color-bg-secondary)] transition-colors w-full sm:w-auto text-center"
        >
          <span class="sm:hidden">Achievements</span>
          <span class="hidden sm:inline">View achievements</span>
        </a>
      </div>
    </section>

    {allPosts.length > 0 && (
      <section>
        <h2 class="text-3xl font-bold mb-8">Latest Posts</h2>
        <div class="space-y-8">
          {allPosts.map((post) => (
            <article class="group">
              <a href={`/en/blog/${post.slug.replace(/-en$/, '')}`} class="block">
                <time class="text-sm text-[var(--color-text-secondary)]">
                  {post.data.pubDate.toLocaleDateString('en-US', {
                    year: 'numeric',
                    month: 'long',
                    day: 'numeric'
                  })}
                </time>
                <h3 class="text-2xl font-semibold mt-2 mb-3 group-hover:text-primary-400 transition-colors">
                  {post.data.title}
                </h3>
                <p class="text-[var(--color-text-secondary)]">
                  {post.data.description}
                </p>
              </a>
            </article>
          ))}
        </div>
        <div class="mt-12">
          <a 
            href="/en/blog" 
            class="inline-flex items-center text-primary-400 hover:underline"
          >
            View all posts →
          </a>
        </div>
      </section>
    )}
  </div>
</BaseLayout>
